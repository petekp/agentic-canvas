# OpenAI API Key for chat functionality
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Supermemory API Key for persistent memory and insights
# Get your key at: https://supermemory.ai/
SUPERMEMORY_API_KEY=sm_...

# Optional: pi runtime seam settings (phase-1 backend orchestration)
# Runtime root for episodes/ledger/snapshots/memory session artifacts
PI_RUNTIME_ROOT=.runtime/pi
# Optional external runtime engine module (absolute path, relative path, file:// URL, or package name)
# Example: file:///Users/you/Code/pi-mono/packages/runtime/dist/engine.mjs
PI_RUNTIME_ENGINE_MODULE=
# Optional named export from module (defaults to auto-detect: piRuntimeEngine/default/stream)
PI_RUNTIME_ENGINE_EXPORT=
# Optional pi-mono model selection for external runtime engine modules
PI_MONO_PROVIDER=openai
PI_MONO_MODEL=gpt-4o
# Optional dry-run mode for pi-mono engine adapters ("1" emits deterministic local output)
PI_MONO_DRY_RUN=
# Optional: disable filesystem persistence entirely ("1" disables)
PI_EPISODE_LOG_DISABLED=
# Optional: enable runtime diagnostics endpoint at GET /api/pi/runtime ("1" or "true")
PI_RUNTIME_DIAGNOSTICS_ENABLED=
# Optional retention scheduler interval in milliseconds (default 3600000)
PI_RETENTION_INTERVAL_MS=
# Optional bearer token for POST /api/pi/retention
PI_RETENTION_API_TOKEN=

# Optional: local filesystem-tool testing profile (recommended for prototype E2E/smoke runs)
# Set PI_FS_ALLOWED_ROOT to a dedicated sandbox directory inside your repo or temp workspace.
# Example: PI_FS_ALLOWED_ROOT=/Users/you/Code/agentic-canvas/.runtime/pi-fs-sandbox
PI_FILESYSTEM_TOOLS_ENABLED=1
PI_FS_ALLOWED_ROOT=
PI_FS_MAX_READ_BYTES=262144
PI_FS_MAX_WRITE_BYTES=262144
PI_FS_MAX_LIST_ENTRIES=200
PI_FS_MAX_EDIT_OPERATIONS=20
PI_FS_DELETE_ENABLED=0

# GitHub Personal Access Token (required for all GitHub components)
# Get your token at: https://github.com/settings/tokens
# Needs: repo (read) scope for private repos, or no scopes for public repos
GITHUB_TOKEN=ghp_...

# Slack Bot Token (optional, for Slack integration)
# Create a Slack app at: https://api.slack.com/apps
# Bot Token Scopes needed: channels:history, channels:read, users:read
# Note: Slack mentions require a **User OAuth token (xoxp-)** with search:read.
# Bot tokens cannot use the search API. Use channel activity + a transform as a workaround.
# Install to workspace, then copy Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-...
# Optional: User OAuth Token (for Slack mentions via search API)
SLACK_USER_TOKEN=xoxp-...

# Vercel Access Token (optional, for Vercel deployment monitoring)
# Create a token at: https://vercel.com/account/tokens
VERCEL_TOKEN=...
# Optional: Project and Team IDs for default project context
# Find these in your Vercel project settings
VERCEL_PROJECT_ID=prj_...
VERCEL_TEAM_ID=team_...
