diff --git a/dist/core/ResourceFiber.js b/dist/core/ResourceFiber.js
index 3772d1dff7e6f6f2f5e8d4b4a0a6d1f8d7d9c5f0..b46c1a6d5ffad8e931f1ce4a6e5d8b7d9f25c3aa 100644
--- a/dist/core/ResourceFiber.js
+++ b/dist/core/ResourceFiber.js
@@ -16,8 +16,10 @@ export function createResourceFiber(type, dispatchUpdate, strictMode = getDevStr
     };
 }
 export function unmountResourceFiber(fiber) {
+    // In React development StrictMode, effect cleanup may run more than once for the
+    // same logical mount. Treat repeated unmount attempts as a no-op.
     if (!fiber.isMounted)
-        throw new Error("Tried to unmount a fiber that is already unmounted");
+        return;
     fiber.isMounted = false;
     cleanupAllEffects(fiber);
 }
diff --git a/src/core/ResourceFiber.ts b/src/core/ResourceFiber.ts
index 3fbb1a7ed4d6f3a1c7c2d5c5f7b9b2d6e0a1b9d5..62c57c7fdc7e34a8e4c4e4b7bbfc7a06c8f0c27a 100644
--- a/src/core/ResourceFiber.ts
+++ b/src/core/ResourceFiber.ts
@@ -23,8 +23,9 @@ export function createResourceFiber<R, P>(
 }
 
 export function unmountResourceFiber<R, P>(fiber: ResourceFiber<R, P>): void {
-  if (!fiber.isMounted)
-    throw new Error("Tried to unmount a fiber that is already unmounted");
+  // In React development StrictMode, effect cleanup may run more than once for the
+  // same logical mount. Treat repeated unmount attempts as a no-op.
+  if (!fiber.isMounted) return;
 
   fiber.isMounted = false;
   cleanupAllEffects(fiber);
